name: 'Publish'

on:
  push:
    tags:
      - 'v*'

jobs:
  publish:
    environment: production
    name: 'Publish to npm'
    runs-on: 'ubuntu-latest'
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: 'actions/checkout@v6'
        with:
          fetch-depth: 0

      - uses: 'moonrepo/setup-toolchain@v0'
        with:
          auto-install: true

      - run: 'bun install --frozen-lockfile'
      - run: 'moon run std:build server:build --color'
      - run: 'bun install -g npm'

      - run: 'bun pm pack'
        working-directory: 'packages/std'
      - run: 'npm publish packages/std/*.tgz --access public --provenance'

      - run: 'bun pm pack'
        working-directory: 'packages/server'
      - run: 'npm publish packages/server/*.tgz --access public --provenance'
