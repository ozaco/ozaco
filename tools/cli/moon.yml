language: "typescript"
type: "library"
platform: "bun"

tasks:
  build:
    command: "bun run src/main.ts build -s"
    inputs:
      - "src/**/*"
      - "packages/**/*"
    outputs:
      - "dist/**"
