// --- Standard Schema (https://standardschema.dev) --------------------------------------------------

/** The Standard Schema v1 contract — any conforming validator (zod v4, valibot, arktype, …) can be
 * used as a command action's `input` schema. Re-exported from `std:shared` (the single vendored
 * copy) under cli's established name. */
export type { StandardSchemaV1 } from 'std:shared'
