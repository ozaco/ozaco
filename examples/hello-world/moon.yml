language: typescript
layer: library
toolchains:
  default: bun

tasks:
  build:
    command: bun x tsdown
    inputs:
      - src/**/*
      - tsdown.config.ts
    outputs:
      - dist/**/*
  run:
    command: bun run src/index.ts
    deps:
      - std:build
    options:
      runInCI: false
