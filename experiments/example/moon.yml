language: "typescript"
type: "library"
platform: "bun"

tasks:
  build:
    command: "bunx ozaco build"
    deps:
      - "cli:build"
      - "std:build"
    inputs:
      - "src/**/*"
    outputs:
      - "dist/**"
